# Obviously the following is AI Generated

# Contributing to ScholarSphere

## Setup

1. Create a virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Run locally using `streamlit run app.py`.

## Guidelines

- Keep changes focused and minimal.
- Follow existing code style and naming.
- Update `README.md` if behavior/setup changes.
- Do not commit generated files (cache, virtual env, secrets).

## Pull Request Checklist

- [ ] Code runs locally
- [ ] No secrets or local artifacts committed
- [ ] Documentation updated if needed